# Handoff — 2026-08-05

## Session Summary

Implemented tickets **#34** and **#35** — scaffold + schema detection. The app now starts, serves a React frontend, scans local directories for shapefiles, detects their schema (CRS, columns, geometry type, validity), and previews rows with column mapping applied.

## Completed Tickets

### ✅ #34 — 01 Scaffold + DuckLake foundation

**What was built:**
- FastAPI backend with `/api/health`, DuckDB in-memory connection, DuckLake ATTACH
- Catalog abstraction: DuckDB by default, PostgreSQL via `VWORLD_CATALOG_TYPE=postgresql` env var
- React 19 + shadcn/ui frontend (dark theme), served from FastAPI in production
- Health dashboard showing DuckLake extension status + catalog connectivity

**Key files:**
- `backend/main.py` — FastAPI app entry point
- `backend/catalog.py` — Catalog abstraction (DuckDB | PostgreSQL)
- `backend/ducklake_ops.py` — DuckLake extension setup + ATTACH
- `frontend/src/App.tsx` — Health dashboard
- `requirements.txt` — Python deps (fastapi, uvicorn, duckdb, duckle, httpx, bs4)

### ✅ #35 — 02 Local directory + schema detection + editor

**What was built:**
- `POST /api/scan-directory` — scans a local path for `.zip` and `.shp` files
- `POST /api/detect-schema` — reads shapefile via DuckDB `ST_Read`, returns CRS, columns, geometry type, row count, geometry validity counts
- `POST /api/preview` — reads first N rows with column rename/drop mapping applied
- File grid with checkboxes, Select All, text filter
- Schema editor with per-column rename inputs + drop toggles
- Preview table showing mapping results

**Key files:**
- `backend/schema_detector.py` — DuckDB Spatial-based schema detection (no fiona/GDAL)
- `frontend/src/components/DirectoryPicker.tsx` — Path input + drag-and-drop zone
- `frontend/src/components/FileGrid.tsx` — Scanned file list with selection
- `frontend/src/components/SchemaEditor.tsx` — Column editor + preview

## Decision Change from ADR

**Shapefile reading uses DuckDB Spatial, not fiona/GDAL.** The original plan was fiona, but DuckDB's `ST_Read()` handles everything we need (CRS via `ST_CRS`, geometry type via `ST_GeometryType`, validity via `ST_IsValid`) with zero extra dependencies. This aligns with ADR #2's zero-dependency principle. `.zip` files are unzipped to a temp directory first (stdlib `zipfile`), then read as `.shp`.

### ✅ #36 — 03 Pipeline execution

**What was built:**
- `backend/pipeline/runner.py` — Duckle pipeline runner wrapping the full flow
- `POST /api/run-pipeline` endpoint + frontend "Run Pipeline" button
- `GET /api/tables` + `GET /api/tables/{name}` — quick DuckLake data viewer

**Gotchas discovered:**
- `qa.geomvalidate` mode values are `"valid"` / `"invalid"` (not `"keep_valid"` / `"keep_invalid"`)
- `p.rename(col="new_name")` works; `p.transform("xf.rename", col="new_name")` does NOT
- Shapefiles can't mix geometry types — GDAL converts all to the first type found
- DuckLake doesn't support indexes; post-crawl merge + spatial index are non-fatal
- **`.zip` files:** duckle's `src.spatial` can't read `.zip` directly. The runner imports `_unzip_shapefile` from `schema_detector.py` to extract `.zip` → `.shp` before passing to duckle

**Data in catalog:** `roads` (4 rows), `buildings` (2 rows) at `catalog/ducklake_metadata.ducklake`

## Next Session: Implement

Continue with **#37 — Real-time pipeline progress**. This ticket adds WebSocket streaming from the pipeline runner to the frontend, showing per-node status (e.g., "src.spatial ✓ (234,051 rows)", "qa.geomvalidate ⏳") and live reject counter.

## Remaining Tickets

| # | Title | Status |
|---|-------|--------|
| #36 | 03 — Pipeline execution | 🔜 next |
| #37 | 04 — Real-time pipeline progress | |
| #38 | 05 — DuckLake console | |
| #39 | 06 — Crawler (login + discovery + download) | |
| #40 | 07 — Incremental re-crawl + map preview | |

## Key Artifacts (unchanged from previous handoff)

| Artifact | Location |
|----------|----------|
| Domain glossary | `CONTEXT.md` |
| Architecture | `docs/ARCHITECTURE.md` |
| Spec (33 user stories) | `docs/spec.md` |
| ADR: Duckle as library | `docs/adr/0001-duckle-as-library.md` |
| ADR: DuckDB catalog | `docs/adr/0002-duckdb-catalog.md` |
| ADR: One table per dataset | `docs/adr/0003-one-table-per-dataset.md` |
| ADR: Web console + Wails viewer | `docs/adr/0004-web-console-wails-viewer.md` |
| Previous handoff | `docs/handoff/2026-08-04.md` |

## Running the App

```bash
# Backend
cd backend && python main.py

# Frontend dev (with HMR)
cd frontend && npm run dev

# Production (backend serves built frontend)
cd frontend && npm run build && cd ../backend && python main.py
```

Open http://127.0.0.1:8000 for the GUI, http://127.0.0.1:8000/docs for API docs.
